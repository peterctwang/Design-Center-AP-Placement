# AI Wall Design — 技術經驗 / 踩坑紀錄

> 從 Desktop GUI 移植到 Web 過程中累積的關鍵技術與「血淚」教訓。
> 本文件補充「技術棧規劃書_輕量版.md」，重點記錄**怎麼避免重複踩坑**。

---

## 目錄
1. [關鍵架構決策](#1-關鍵架構決策)
2. [牆體偵測八大踩坑](#2-牆體偵測八大踩坑)
3. [前端 Canvas 對齊踩坑](#3-前端-canvas-對齊踩坑)
4. [GA 演算法調校經驗](#4-ga-演算法調校經驗)
5. [自製平面圖規則](#5-自製平面圖規則)
6. [其他系統性教訓](#6-其他系統性教訓)
7. [Quick Reference 速查表](#7-quick-reference-速查表)

---

## 1. 關鍵架構決策

### 1.1 「為什麼不用 Docker」
- 目標是 **3 個命令啟動 + 跨機器 git clone 可移植**
- Docker 帶來 200MB+ image + Docker Desktop 安裝門檻
- 改用 **venv + npm install**：對方只需要裝 Python 3.11 + Node 20

### 1.2 「為什麼前端 build 進 backend/static」
- 一個 `uvicorn` 進程同時 serve API + SPA
- 對方不需要 Node.js 也能跑（只要拿 build 好的版本）
- 部署 = 複製整個 backend 資料夾即可

### 1.3 「為什麼不用 Celery」
- 單機 demo 不需要分散式 task queue
- `FastAPI BackgroundTasks` + `threading.Thread` + `asyncio.Queue` 已足夠
- 進度推送用 **WebSocket + Redis pub/sub 替代品（in-memory queue）**

### 1.4 「為什麼用 SQLite」
- 單檔零安裝
- 對 < 100 並發完全夠用
- 升級到 PostgreSQL 只改 1 行 `DATABASE_URL`

---

## 2. 牆體偵測八大踩坑

### 🪤 踩坑 #1：HoughLinesP 偵測「粗線」會抓到兩條邊
**症狀**：牆有 5px 寬，偵測結果出現上下兩條平行線（雙重線）。

**根因**：HoughLinesP 偵測**邊緣**而非中心，粗線的上下邊緣會被當成兩條獨立線。

**解法**：偵測前**骨架化（skeletonize）**：
```python
from skimage.morphology import skeletonize
skel = skeletonize(mask > 0).astype(np.uint8) * 255
lines = cv2.HoughLinesP(skel, ...)
```
→ 5px 粗線變 1px 中心線 → 偵測結果單一條線

### 🪤 踩坑 #2：BBox 抓「最外緣」造成對齊偏移
**症狀**：偵測牆比圖片牆**內縮 3-5 像素**，視覺上看起來小一圈。

**根因**：用 `cv2.findNonZero + boundingRect` 抓到**牆的外緣**，但偵測 skeleton 抓的是**牆的中心**，兩者偏差 = 牆厚/2。

**解法**：BBox 抓「**牆中心**」位置：
```python
# 對每一列，找最左暗像素 + 從此延伸的連續暗像素長度的一半
for y in range(h):
    idx = np.where(mask[y] > 0)[0]
    run_length = count_consecutive(mask[y], idx[0])
    left_centres.append(idx[0] + run_length // 2)
x0 = int(np.median(left_centres))
```

### 🪤 踩坑 #3：標題與比例尺被當成建築範圍
**症狀**：bbox 包含標題列「Hotel Floor」和 scale bar「5 m」→ px/m 比例計算錯誤。

**根因**：圖片不只有建築，還有 title、scale bar、邊距等「裝飾」。

**解法**：用**中位數**（不是 min/max）找邊界。標題、比例尺只在少數行出現，中位數會自動忽略這些 outlier。

### 🪤 踩坑 #4：Hough 把一道長牆切成許多短段
**症狀**：一道直牆變成 5-10 個短段，每段都有兩個端點，畫面像「點點」。

**解法**：偵測後**合併共線段**：
```python
# 兩段視為同一條線的條件：
- 角度差 < 8°
- 垂直偏移 < 12 px
- 沿線間隙 < 80 px (可跨過門洞)
```
→ 同一條線上的多段合併成單一長段

### 🪤 踩坑 #5：玻璃窗破壞外牆連續性
**症狀**：用 `RETR_EXTERNAL contour` 找最大連通元件失敗，因為玻璃窗（青色）切斷了黑色外牆。

**解法**：BBox 偵測用**聯集所有材質的 mask**（concrete + brick + glass + metal + door），不只用 concrete。

### 🪤 踩坑 #6：上下牆厚度不對稱
**症狀**：matplotlib 用 `plot([x1,x2],[y1,y2], linewidth=5)` 畫的線，**渲染時上下厚度可能不一致**（hotel 圖出現過 top=3px, bot=7px）。

**解法**：用**填滿矩形** (`patches.Rectangle`) 畫外牆，保證像素精確一致：
```python
ax.add_patch(patches.Rectangle((0, 0), W, T, facecolor='black'))         # bottom
ax.add_patch(patches.Rectangle((0, H - T), W, T, facecolor='black'))     # top
ax.add_patch(patches.Rectangle((0, 0), T, H, facecolor='black'))         # left
ax.add_patch(patches.Rectangle((W - T, 0), T, H, facecolor='black'))     # right
```

### 🪤 踩坑 #7：HSV 顏色範圍要寬鬆
**症狀**：偵測 door 時 0 個結果，但圖中明明有綠色門。

**根因**：JPEG 壓縮 / 抗鋸齒 / 矩陣轉換造成顏色微飄。原本範圍 `H:40-70` 抓不到實際 H≈44 的綠色。

**解法**：放寬 HSV 範圍 + 增加邊界容忍度：
```python
'door':     (np.array([35, 60, 60]), np.array([80, 255, 240])),
```

### 🪤 踩坑 #8：MIN_LINE_PX 太大抓不到小門
**症狀**：apartment 圖的 0.8m 門（~32 px）偵測不到。

**解法**：MIN_LINE_PX 從 30 降到 **18**，能抓到小門但仍過濾雜訊。

---

## 3. 前端 Canvas 對齊踩坑

### 🪤 踩坑 #9：`stageH = max(300, w*aspect)` 強制最小高度
**症狀**：寬扁建築（如 hotel 40×12）的牆畫到 canvas 89% 處就停了，下方 11% 留白，牆和圖片不對齊。

**根因**：
```js
setStageH(Math.max(300, w * aspect));   // 強制最小 300px
// 但牆座標用 scale = stageW / buildingW，沒被 clamp
```
寬扁建築 `w*aspect < 300` 被 clamp 到 300，但牆 scale 不變 → **圖片高度 ≠ 牆對應高度**

**解法**：**永遠按 building aspect 比例**，不能 clamp。如果太矮就降低 stageW：
```js
let w = parentW;
let h = w * aspect;
if (h > maxH) {
  h = maxH;
  w = h / aspect;  // 反推寬度保比例
}
```

### 🪤 踩坑 #10：圖片快取讓「Replace image」看起來無效
**症狀**：上傳新圖後畫面還是舊圖。

**解法**：URL 加 cache-buster：
```js
const imageUrl = `/uploads/${id}.png?t=${project.updated_at}`;
```
`updated_at` 每次後端更新時變動 → 強制重新載入

### 🪤 踩坑 #11：上傳新圖時舊牆/AP 沒清掉
**症狀**：換圖後牆和 AP 座標都對不上新圖。

**解法**：upload endpoint **同時清掉** walls / aps / 舊圖檔（含不同副檔名）：
```python
for ext in (".png", ".jpg", ".jpeg", ".bmp", ".gif"):
    (upload_dir / f"{project_id}{ext}").unlink(missing_ok=True)
db.query(Wall).filter(Wall.project_id == pid).delete()
db.query(AccessPoint).filter(AccessPoint.project_id == pid).delete()
```

---

## 4. GA 演算法調校經驗

### 🪤 踩坑 #12：自由空間 FSPL 模型對室內過度樂觀
**症狀**：30m 距離 RSSI 算出 -49 dBm（太強），實際室內應該 -64 dBm。

**解法**：改用**室內 log-distance 模型**：
```python
PL(d) = 40 + 10 * n * log10(d)
n = 3.0 (office), 3.5 (warehouse)
# 不是 FSPL 的 n = 2.0
```

### 🪤 踩坑 #13：GA 在密集牆面下卡住
**症狀**：office 有 100 道牆，GA 跑 30 代後 coverage = 0%。

**原因**：
1. `min_wall_dist = 1.0m` 太嚴 → 密集格局裡找不到「離牆夠遠」的點
2. 牆 + 容量懲罰 + 跨牆衰減 → fitness 函式幾乎都是負分

**解法**：
- `min_wall_dist` 降到 **0.5m**
- `min_ap_sep` 從 4m 降到 **3m**
- 增加 population 50 + 40 代演化

### 🪤 踩坑 #14：Auto-N 從 N=1 開始給出 1 個 AP 的偽解
**症狀**：建築面積 600m² 但 GA 給 1 個 AP 號稱 97% 覆蓋。

**根因**：
1. 邊界用「偵測牆框」而非「圖片真實大小」→ 範圍縮小 → 1 AP 真的能覆蓋
2. Auto-N 從 N=1 開始 → 第一個達標就停

**解法**：
- 邊界改用**「圖片實際 building_extent_m」**
- Auto-N 引入**容量公式**：`min_n = max(2, ceil(area / 120))`
  - 120 m²/AP 是業界標準（Cisco/Aruba 建議）
  - 對 600m² 起算 N=5

### 🪤 踩坑 #15：GA Python 迴圈太慢
**症狀**：office 13 個 AP 跑 60 秒。

**解法**：**向量化 RSSI 計算**：
```python
# 不要 for 每個 sample point 算每道牆
# 改用一次 NumPy 矩陣運算
A = ap[None, None, :]; B = P[:, None, :]
W1b = W1[None, :, :]; W2b = W2[None, :, :]
intersects = ccw_test(A, B, W1b, W2b)  # (N points, M walls) 一次算完
wall_loss = (intersects * LOSS[None,:]).sum(axis=1)
```
→ **7-10 倍加速**（60s → 12s）

### 🪤 踩坑 #16：asyncio.get_event_loop() 在 sync handler 失敗
**症狀**：FastAPI 同步路由 + threading worker，呼叫 `asyncio.get_event_loop()` 拋 `RuntimeError: no current event loop`。

**根因**：FastAPI 的 sync handler 跑在 anyio worker thread，沒有 event loop。

**解法**：endpoint 改 **async def** + 用 `asyncio.get_running_loop()`：
```python
@router.post("/optimize")
async def start_optimize(...):
    loop = asyncio.get_running_loop()  # 在 async 上下文
    hub.register_loop(task_id, loop)
```

---

## 5. 自製平面圖規則

如果你要自己畫平面圖讓系統能正確偵測 + 對齊，遵循這些規則：

### 5.1 檔名格式（必要）
```
{name}_{W}x{H}m.png
```
範例：`office_50x30m.png`、`apartment_12x9m.png`
- 系統會用 regex 自動讀取建築尺寸
- 沒檔名提示時預設 20m，會跳對話框問

### 5.2 顏色規範（每種材質一色）
| 材質 | HEX | dB 衰減 | 用途 |
|------|------|--------|------|
| concrete | `#000000` 黑 | 12 | 外牆 / 結構柱 |
| brick | `#8B4513` 棕 | 6 | 室內隔間 |
| glass | `#00CED1` 青 | 3 | 窗戶 |
| wood | `#D2691E` 橘棕 | 4 | 木板門/家具 |
| drywall | `#A9A9A9` 中灰 | 3 | 輕隔間 |
| metal | `#4B4B4B` 深灰 | 20 | 金屬機櫃/分隔 |
| door | `#00B050` 綠 | 3 | 房間門 |

⚠️ **務必使用上述精確顏色**（顏色容差有限），自由配色會被誤判。

### 5.3 牆繪製規則
**外牆**：必須用**填滿矩形**確保 4 面厚度一致：
```python
ax.add_patch(patches.Rectangle((0, 0), W, T, facecolor=CONCRETE))      # bottom
ax.add_patch(patches.Rectangle((0, H-T), W, T, facecolor=CONCRETE))    # top
ax.add_patch(patches.Rectangle((0, 0), T, H, facecolor=CONCRETE))      # left
ax.add_patch(patches.Rectangle((W-T, 0), T, H, facecolor=CONCRETE))    # right
```
- 厚度 T 建議 0.2–0.3m
- 別用 `plot(...linewidth=)` 因為渲染厚度可能不對稱

**內牆**：可以用 `plot` + linewidth=3：
```python
ax.plot([x1,x2], [y1,y2], color=BRICK, linewidth=3, solid_capstyle='butt')
```

**門**：綠色實線 + 弧形開啟符號：
```python
ax.plot([cx-0.5, cx+0.5], [cy, cy], color=DOOR, linewidth=5)
theta = np.linspace(0, np.pi/2, 30)
ax.plot(cx + np.cos(theta), cy + np.sin(theta),
        color=DOOR, linewidth=0.8, linestyle='--', alpha=0.7)
```

### 5.4 邊距規則
- Title 放在建築**上方 ≥ 1m**（避免被當成建築結構）
- Scale bar 放在建築**下方 ≥ 1m**
- 周圍留**白邊 ≥ 0.3 inch**（matplotlib `pad_inches=0.3`）
- `bbox_inches='tight'` 自動裁掉多餘空白

### 5.5 字體與標記
- 房間標籤用**淺灰小字**（不要太黑，避免被當牆）
- 數字標記放房間中心，遠離牆面
- 避免在建築範圍內畫紅色 / 黑色裝飾線

### 5.6 推薦的圖片解析度
- dpi = 120
- figsize 按 (W*0.4, H*0.4) 計算（每米 0.4 inch）
- 最終 PNG 約 2000×1000 像素 — 偵測精度足夠且檔案不大

---

## 6. 其他系統性教訓

### 6.1 「先驗證再優化」
PoC 階段直接拿 Desktop 既有的 `genetic_optimizer.py` + `wall_detector.py` 100% 複用，省下 60% 演算法開發時間。

### 6.2 「Test 在實機而非合成資料」
合成測試圖（matplotlib 生成）跟真實照片差距很大。**SAM 整合**是 Phase 2 必做。

### 6.3 「對齊問題是綜合性的」
單一 bug 修了仍會偏移 → 整個 pipeline（生圖 → 裁切 → 偵測 → 渲染）每個環節都得對齊：
```
生圖：對稱填滿矩形
↓
裁切：bbox 抓牆中心位置
↓
偵測：skeleton 1px 中心線
↓
渲染：stageH 嚴格按 aspect 計算
```
任何一步出錯，視覺上就會偏移。

### 6.4 「快速重啟流程」
Windows 上殺 Python 進程 + 重啟伺服器的常見一行：
```bash
netstat -ano | grep ":8000.*LISTEN" | awk '{print $5}' | head -1 |
  xargs -I {} cmd //c "taskkill /F /PID {}"
sleep 3 && uvicorn app.main:app --port 8000
```

### 6.5 「前端改完一定要 `npm run build`」
否則 backend/static 還是舊版。**這是踩過最多次的坑**。

---

## 7. Quick Reference 速查表

### 7.1 演算法常數（業界標準）

| 常數 | 值 | 來源 |
|------|---|------|
| 室內 path loss exponent | n = 3.0 | Cisco WiFi Design Guide |
| Reference path loss @ 1m, 2.4GHz | 40 dB | Friis equation |
| Coverage threshold | -65 dBm | -65 = decent, -55 = good |
| 容量密度 | 120 m²/AP | Cisco enterprise default |
| AP 最小間距 | 3 m | 防止干擾重疊 |
| AP 離牆最小距離 | 0.5 m | 安裝空間 + 物理 |

### 7.2 GA 參數

```python
optimize_ap_placement_auto(
    bounds=..., walls=...,
    target_coverage=0.9,      # 達標即停
    min_n=2, max_n=16,        # AP 數搜尋範圍
    sqm_per_ap=120.0,         # 容量公式
    population_size=50,
    n_generations=40,
    sample_resolution=20,      # 20×20 = 400 個取樣點
)
```

### 7.3 偵測參數

```python
MIN_LINE_PX = 18              # 最短可接受牆段
MATERIAL_HSV_RANGES = {...}    # 6 種材質 HSV 範圍
merge: angle_tol=8°, offset=12px, gap=80px
auto_crop: 啟用，bbox 抓「中位數連續暗像素中心」
```

### 7.4 系統執行 3 命令

```bash
# 1. 安裝
pip install -r requirements.txt
npm install && npm run build

# 2. 啟動
uvicorn app.main:app --port 8000
```

→ 開瀏覽器 http://localhost:8000

---

## 結語

本文件源自從 Desktop GUI 移植到 Web 過程中的**真實除錯經驗**。建議：

- 後續修 bug 前先看 §2、§3 是否已記錄
- 加新功能前看 §5 規範
- 自動化測試應該覆蓋所有踩坑點（建議用這些 sample 圖當 fixture）

**最後一句金句**：
> 「不對齊問題從來不是單一原因 — 永遠是『生圖 → 偵測 → 渲染』三方任一步偏差的累積。」
