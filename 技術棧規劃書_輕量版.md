# AI Wall Design Web — 輕量版技術棧規劃書

> **設計原則**：快速 Demo / 高可移植 / 零 Docker / 最大化複用 Desktop 程式碼
>
> **目標**：別人 git clone 後，**3 個命令** 即可在自己機器跑起來。

---

## 0. 設計取捨

| 項目 | 完整版（前版） | **輕量版（本版）** | 取捨原因 |
|------|--------------|-----------------|---------|
| 認證 | JWT + OAuth | **無**（單機/Demo） | 不用做登入 |
| 容器 | Docker + K8s | **無，純 venv + node_modules** | 直接移植 |
| DB | PostgreSQL + PostGIS | **SQLite**（單檔） | 零安裝 |
| Cache | Redis | **Python dict / SQLite** | 不用裝 |
| Task Queue | Celery + Redis | **FastAPI BackgroundTasks + threading** | 不用裝 |
| 物件儲存 | S3 / MinIO | **本機 `data/` 資料夾** | 不用裝 |
| WebSocket | socket.io | **FastAPI 原生 WebSocket** | 已內建 |
| 前端建置 | 獨立 Vite dev server | **build 後由 FastAPI 直接 serve** | 單一執行檔 |
| 部署 | K8s + ALB + CDN | **`uvicorn` 一個指令** | 單機跑 |

---

## 1. 系統架構（極簡）

```
┌──────────────────────────────────────────────────┐
│              使用者瀏覽器                          │
│  http://localhost:8000                            │
└──────────────────┬───────────────────────────────┘
                   │
        ┌──────────▼──────────┐
        │   FastAPI 後端       │   ← 單一 Process
        │   port 8000          │
        │                      │
        │  /api/*  →  REST     │
        │  /ws/*   →  WebSocket│
        │  /*      →  React 靜態檔案（built dist/） │
        │                      │
        │  BackgroundTasks     │   ← GA, CV 在背景 thread
        │  (no Celery)         │
        └──────┬───────┬───────┘
               │       │
        ┌──────▼─┐ ┌──▼──────────┐
        │SQLite  │ │ 本機檔案系統 │
        │ app.db │ │ data/        │
        │        │ │  uploads/    │
        │        │ │  reports/    │
        └────────┘ └──────────────┘

複用 Desktop 既有程式碼：
  algorithms/genetic_optimizer.py   ← 100% 不改
  algorithms/wall_detector.py        ← 100% 不改
```

---

## 2. 後端技術棧

### 2.1 核心
```yaml
runtime:    Python 3.11+        # 跟 Desktop 版同
manager:    pip + venv         # 不用 Poetry，純標準
server:     uvicorn 0.30        # 開發+生產同一個
```

### 2.2 套件清單（`backend/requirements.txt`）
```txt
# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9       # 檔案上傳
websockets==13.0               # WebSocket
aiofiles==24.1.0               # 非同步檔案

# ORM（輕量）
sqlalchemy==2.0.35             # 同步模式 + sqlite 即可
alembic==1.13.3                # （可選）migration

# Pydantic
pydantic==2.9.2
pydantic-settings==2.5.2

# === 演算法（複用 Desktop 版的依賴） ===
numpy>=1.21.0
opencv-python-headless==4.10.0.84
pillow>=10.0.0
scikit-image>=0.18.0
deap>=1.3.1
matplotlib>=3.5.0              # PDF 報表使用

# PDF 報表
reportlab==4.2.5               # 比 WeasyPrint 簡單

# 工具
python-dotenv==1.0.1
loguru==0.7.2                  # 日誌
```

**只有 13 個套件**，全部 pip 可裝，無系統依賴（除 OpenCV 在 Linux 需 `libgl1`）。

### 2.3 為何選 SQLite？
- 單一檔案 `app.db`，零安裝
- Python 標準庫內建，pip 不用裝
- 單機 demo 完全夠用（10 萬筆專案沒問題）
- 想升 PostgreSQL 只改 1 行：`DATABASE_URL=sqlite:///app.db` → `postgresql://...`

### 2.4 為何不用 Celery？
| 需求 | Celery 解決 | 輕量替代 |
|------|------------|---------|
| 長時間任務不阻塞 API | task queue | **FastAPI BackgroundTasks** |
| 進度推送 | celery + redis pubsub | **WebSocket + threading.Event** |
| 失敗重試 | celery retry | 手動 try/except + Log |
| 跨機器分散 | broker | 不需要（單機） |

範例：用 BackgroundTasks 跑 GA
```python
from fastapi import BackgroundTasks, WebSocket
import asyncio, queue

@app.post("/api/projects/{pid}/optimize")
async def start_ga(pid: str, params: OptimizeReq, bg: BackgroundTasks):
    task_id = uuid4().hex
    progress_queues[task_id] = queue.Queue()
    bg.add_task(run_ga_worker, task_id, pid, params)
    return {"task_id": task_id, "ws_url": f"/ws/tasks/{task_id}"}

def run_ga_worker(task_id, pid, params):
    walls = db.get_walls(pid)
    def cb(gen, cov):
        progress_queues[task_id].put({"progress": gen, "coverage": cov})
    result = optimize_ap_placement_auto(..., progress_cb=cb)
    db.save_aps(pid, result)
    progress_queues[task_id].put({"done": True, "result": result})

@app.websocket("/ws/tasks/{task_id}")
async def stream(ws: WebSocket, task_id: str):
    await ws.accept()
    q = progress_queues[task_id]
    while True:
        msg = await asyncio.to_thread(q.get)
        await ws.send_json(msg)
        if msg.get("done"): break
```

---

## 3. 前端技術棧

### 3.1 核心
```yaml
runtime:    Node.js 20 LTS
manager:    npm（內建，不依賴 pnpm/yarn）
framework:  React 18 + TypeScript
build:      Vite 5
```

### 3.2 套件清單（`frontend/package.json`）
```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.26.0",
    "axios": "^1.7.0",
    "konva": "^9.3.0",
    "react-konva": "^18.2.0",
    "plotly.js-dist-min": "^2.35.0",
    "react-plotly.js": "^2.6.0",
    "zustand": "^4.5.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
```

**只有 10 個 production 套件**。

### 3.3 拿掉的東西（vs 完整版）
- ❌ TanStack Query（用 axios + useState 即可）
- ❌ React Hook Form + Zod（簡單表單）
- ❌ shadcn/ui（用 Tailwind + 純 HTML）
- ❌ Three.js（先做 2D，3D 用 Plotly 內建）
- ❌ socket.io（用瀏覽器原生 WebSocket）
- ❌ Sentry / 監控

### 3.4 前端建置 → 由後端 serve
```typescript
// vite.config.ts
export default {
  build: {
    outDir: '../backend/static',   // 直接 build 到後端的 static 資料夾
    emptyOutDir: true,
  },
};
```

```python
# backend/app/main.py
from fastapi.staticfiles import StaticFiles
app.mount("/", StaticFiles(directory="static", html=True), name="static")
```

→ build 後**整個前端變成後端的一部分**，使用者只需要跑 `uvicorn`。

---

## 4. 演算法層（100% 複用）

直接從 Desktop 版複製：
```
backend/algorithms/
  ├── genetic_optimizer.py    ← 從 Desktop /src/ 複製
  └── wall_detector.py        ← 從 Desktop /src/ 複製
```

**完全不改一行程式碼**。

呼叫範例：
```python
# backend/app/services/optimize.py
from algorithms.genetic_optimizer import optimize_ap_placement_auto
from algorithms.wall_detector import detect_walls_from_image

def run_optimization(project_id):
    floor_plan = db.get_floor_plan(project_id)
    walls, info = detect_walls_from_image(
        floor_plan.image_path,
        px_per_meter=floor_plan.scale,
    )
    result, n, cov = optimize_ap_placement_auto(
        bounds=floor_plan.bounds,
        walls=walls,
        target_coverage=0.9,
    )
    return result
```

---

## 5. 資料儲存

### 5.1 SQLite Schema
```python
# backend/app/models.py
from sqlalchemy import Column, String, Float, Integer, DateTime, ForeignKey, JSON
from sqlalchemy.orm import declarative_base, relationship
from datetime import datetime
import uuid

Base = declarative_base()

def uuid_str():
    return uuid.uuid4().hex

class Project(Base):
    __tablename__ = "projects"
    id = Column(String, primary_key=True, default=uuid_str)
    name = Column(String, nullable=False)
    building_type = Column(String)
    image_path = Column(String)            # 本機檔案路徑
    scale_px_per_m = Column(Float)
    building_w_m = Column(Float)
    building_h_m = Column(Float)
    created_at = Column(DateTime, default=datetime.utcnow)
    walls = relationship("Wall", backref="project", cascade="all, delete")
    aps = relationship("AccessPoint", backref="project", cascade="all, delete")

class Wall(Base):
    __tablename__ = "walls"
    id = Column(String, primary_key=True, default=uuid_str)
    project_id = Column(String, ForeignKey("projects.id"))
    p1_x = Column(Float); p1_y = Column(Float)
    p2_x = Column(Float); p2_y = Column(Float)
    material = Column(String, default="concrete")

class AccessPoint(Base):
    __tablename__ = "access_points"
    id = Column(String, primary_key=True, default=uuid_str)
    project_id = Column(String, ForeignKey("projects.id"))
    x = Column(Float); y = Column(Float); z = Column(Float, default=2.7)
    name = Column(String)

class OptimizationRun(Base):
    __tablename__ = "runs"
    id = Column(String, primary_key=True, default=uuid_str)
    project_id = Column(String, ForeignKey("projects.id"))
    num_aps = Column(Integer)
    coverage = Column(Float)
    parameters = Column(JSON)
    created_at = Column(DateTime, default=datetime.utcnow)
```

啟動時自動建表：
```python
# backend/app/db.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from .models import Base

engine = create_engine("sqlite:///./data/app.db", echo=False,
                      connect_args={"check_same_thread": False})
Base.metadata.create_all(engine)
SessionLocal = sessionmaker(bind=engine)
```

### 5.2 檔案佈局
```
data/                           # gitignore
├── app.db                      # SQLite
├── uploads/                    # 平面圖原圖
│   └── {project_id}.png
├── heatmaps/                   # 熱力圖快取
│   └── {run_id}.npz
└── reports/                    # PDF 報表
    └── {project_id}_{ts}.pdf
```

備份只要打包 `data/` 資料夾。

---

## 6. API 設計（無認證）

| Method | Path | 用途 |
|--------|------|------|
| `POST` | `/api/projects` | 建立專案（不需登入） |
| `GET`  | `/api/projects` | 列表（顯示所有，demo 用） |
| `GET`  | `/api/projects/{id}` | 詳細 |
| `DELETE` | `/api/projects/{id}` | 刪除 |
| `POST` | `/api/projects/{id}/upload` | 上傳平面圖 |
| `POST` | `/api/projects/{id}/detect-walls` | 觸發牆偵測（async） |
| `PUT`  | `/api/projects/{id}/walls` | 手動修正牆 |
| `POST` | `/api/projects/{id}/optimize` | 觸發 GA（async） |
| `GET`  | `/api/projects/{id}/heatmap?mode=...` | 取熱力圖矩陣 |
| `GET`  | `/api/projects/{id}/report.pdf` | 下載 PDF |
| `WS`   | `/ws/tasks/{task_id}` | 即時進度 |

**完全沒有 `/auth/`、`/users/`**。

---

## 7. 啟動與部署（3 個命令）

### 7.1 開發
```bash
# 1) 安裝後端
cd backend && python -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# 2) 安裝前端 + 建置
cd ../frontend && npm install && npm run build

# 3) 啟動
cd ../backend && uvicorn app.main:app --reload --port 8000
# → 瀏覽器開 http://localhost:8000
```

### 7.2 跨機器移植（壓縮包模式）
```bash
# 在開發機準備好可移植包
tar czf ai-wall-portable.tar.gz \
    backend/ \
    floor_plans/    # 範例圖
    !backend/venv \
    !backend/data \
    !__pycache__

# 對方機器
tar xzf ai-wall-portable.tar.gz
cd backend
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --port 8000
# 完成！不需要 Docker、node、redis、postgres
```

> 前端 build 後的 `static/` 已在後端內，**對方甚至不需要安裝 Node.js**。

### 7.3 純可執行檔（Optional Phase 2）
用 PyInstaller 打包成單一 .exe：
```bash
pyinstaller --onefile --add-data "static:static" \
            --add-data "data:data" app/main.py
# 產生 dist/ai-wall.exe，雙擊即跑
```

---

## 8. 專案目錄結構（精簡）

```
ai-wall-web/
├── README.md
├── .gitignore
│
├── backend/
│   ├── requirements.txt
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                    # FastAPI app + 掛載靜態檔
│   │   ├── config.py
│   │   ├── db.py                      # SQLite 連線
│   │   ├── models.py                  # SQLAlchemy ORM
│   │   ├── schemas.py                 # Pydantic
│   │   ├── api/
│   │   │   ├── projects.py
│   │   │   ├── walls.py
│   │   │   ├── optimize.py
│   │   │   ├── heatmap.py
│   │   │   └── report.py
│   │   ├── services/
│   │   │   ├── cv_service.py          # 包裝 wall_detector
│   │   │   ├── ga_service.py          # 包裝 genetic_optimizer
│   │   │   └── pdf_service.py
│   │   └── ws/
│   │       └── task_stream.py         # WebSocket
│   ├── algorithms/                    # ← 從 Desktop 複製
│   │   ├── genetic_optimizer.py
│   │   └── wall_detector.py
│   ├── static/                        # ← 前端 build 結果（gitignore）
│   └── data/                          # ← runtime 產生（gitignore）
│
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── pages/
│       │   ├── ProjectList.tsx
│       │   └── Editor.tsx
│       ├── components/
│       │   ├── UploadDropzone.tsx
│       │   ├── FloorPlanCanvas.tsx   # Konva
│       │   ├── HeatmapView.tsx        # Plotly
│       │   ├── ProgressBar.tsx
│       │   └── MaterialSelector.tsx
│       ├── hooks/
│       │   ├── useTaskWS.ts
│       │   └── useProject.ts
│       ├── api/
│       │   └── client.ts
│       └── stores/
│           └── projectStore.ts
│
├── floor_plans/                      # 範例圖（從 Desktop 複製）
│   ├── sample_30x20m.png
│   ├── apartment_12x9m.png
│   ├── office_50x30m.png
│   ├── hotel_40x12m.png
│   └── datacenter_40x25m.png
│
└── scripts/
    ├── setup.sh                       # 一鍵安裝
    ├── setup.bat                      # Windows 版
    └── build.sh                       # 前端 build 進 backend/static
```

---

## 9. 完整啟動腳本

### `scripts/setup.sh`（Linux/Mac）
```bash
#!/usr/bin/env bash
set -e

echo "=== 1. 後端 venv ==="
cd backend
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

echo "=== 2. 前端 build ==="
cd ../frontend
npm install
npm run build      # 會寫到 ../backend/static/

echo "=== 3. 完成 ==="
echo "啟動: cd backend && source venv/bin/activate && uvicorn app.main:app --port 8000"
```

### `scripts/setup.bat`（Windows）
```bat
@echo off
echo === 1. Backend venv ===
cd backend
python -m venv venv
call venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt

echo === 2. Frontend build ===
cd ..\frontend
call npm install
call npm run build

echo === 3. Done ===
echo To start: cd backend ^&^& venv\Scripts\activate ^&^& uvicorn app.main:app --port 8000
```

### `scripts/run.bat`（一鍵啟動）
```bat
@echo off
cd backend
call venv\Scripts\activate
start http://localhost:8000
uvicorn app.main:app --port 8000
```

---

## 10. 程式碼複用對照

| Desktop 原檔 | Web 端用法 | 改動 |
|------------|-----------|------|
| `src/genetic_optimizer.py` | `backend/algorithms/genetic_optimizer.py` | **0 行** |
| `src/wall_detector.py` | `backend/algorithms/wall_detector.py` | **0 行** |
| `gui/wifi_gui_app.py` 內的 `MATERIAL_COLORS`, `MATERIAL_LIST` | `backend/app/constants.py` | 抽出 2 個常數 |
| `floor_plans/*.png` | `floor_plans/*.png` | **0 行** |
| `gui/run_gui.py` 的 `_render_wall_aware_heatmap` 邏輯 | `backend/services/heatmap_service.py` | 拿掉 matplotlib 改回傳 numpy array |
| `gui/run_gui.py` 的 GA threading 邏輯 | `backend/services/ga_service.py` | 改用 BackgroundTasks |
| 其他 tkinter UI | 廢棄 → 改用 React Konva | 重寫 |

**複用率：演算法層 100%、視覺化邏輯 ~70%、UI 0%（要重寫）**

---

## 11. 開發進度（4 週可達 Demo）

### Week 1：骨架 + 第一條鏈路
- [x] Day 1：建專案結構、setup.sh
- [ ] Day 2：FastAPI + SQLite 啟動，掛 React 靜態檔
- [ ] Day 3：POST /upload + 圖檔存 data/uploads
- [ ] Day 4：把 wall_detector 包成 service
- [ ] Day 5：前端拖拉上傳 + 顯示偵測到的牆 JSON

### Week 2：GA 與視覺化
- [ ] Day 6-7：BackgroundTasks 跑 GA + WebSocket 進度
- [ ] Day 8：前端 Konva 平面圖編輯器（顯示牆 + AP）
- [ ] Day 9：Plotly 熱力圖（4 種模式切換）
- [ ] Day 10：覆蓋率報表頁

### Week 3：互動編輯
- [ ] Day 11：Canvas 畫牆、選材質
- [ ] Day 12：手動 AP 拖拉
- [ ] Day 13：比例尺校正
- [ ] Day 14：Project 切換、列表頁

### Week 4：報表與打磨
- [ ] Day 15-16：ReportLab PDF 報表
- [ ] Day 17：5 張範例圖預載
- [ ] Day 18：README + 一鍵啟動腳本
- [ ] Day 19：效能調優
- [ ] Day 20：Demo 影片錄製

---

## 12. 與完整版對照

| 項目 | 完整版 | **輕量版** | 損失 |
|------|--------|----------|------|
| 期程 | 12 週 | **4 週** | -67% |
| 預算 | NT$ 323 萬 | **NT$ 80-100 萬** | -70% |
| 套件數量（後端） | 40+ | **13** | 不需要的拿掉 |
| 套件數量（前端） | 30+ | **10** | 不需要的拿掉 |
| 部署複雜度 | K8s + 7 個服務 | **單一 uvicorn** | 大幅簡化 |
| 跨機器移植 | 需配 Docker / K8s 知識 | **複製資料夾即可** | 大幅簡化 |
| 多人協作 | ✓ | ✗ | 無多使用者 |
| 大型部署 | ✓ scale to 1000s users | 適合 < 100 並發 | 不適合商業上線 |
| 商業化 | ✓ 訂閱制 | 適合內部工具/PoC | 不適合直接收費 |

**這個輕量版適合：**
- 內部工具
- 給客戶展示的 Demo
- PoC / 投標展示
- 顧問公司給自己工程師用
- 升級到完整版前的 MVP 驗證

---

## 13. 升級路徑（Phase 2 商業化）

如果輕量版驗證成功，要商業化只需要：

| 升級項目 | 改動 |
|---------|------|
| SQLite → PostgreSQL | 改 1 行 `DATABASE_URL` |
| BackgroundTasks → Celery | 包現有 service function |
| 本機檔案 → S3 | 改 1 個 service module |
| 加 JWT 認證 | 加 1 個 middleware |
| 加 Stripe | 加 1 組 API endpoint |
| Docker 化 | 加 Dockerfile |

**核心程式（演算法、GA、CV、熱力圖）完全不動**。

---

## 14. README 範本（給接手的人）

```markdown
# AI Wall Design

WiFi AP 自動佈點規劃系統。

## 系統需求
- Python 3.11+
- Node.js 20+（僅編譯前端時需要）

## 安裝（一次）
```bash
git clone <repo>
cd ai-wall-web
./scripts/setup.sh        # Mac/Linux
# 或 scripts\setup.bat     # Windows
```

## 啟動
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --port 8000
```
瀏覽器開 http://localhost:8000

## 已知範例圖
floor_plans/ 目錄下，檔名格式 `name_WxHm.png` 自動讀尺寸。

## 進階：給其他電腦使用
打包：
```bash
tar czf ai-wall-portable.tar.gz backend/ --exclude=venv --exclude=data
```
解壓後重跑 setup.sh，**無需 Docker、PostgreSQL、Redis**。
```

---

**文件結束** — 重點：**3 命令啟動、純資料夾移植、SQLite 單檔、演算法 100% 複用**。
