# AI Wall Design Web — 專案規劃書

> **目標**：將 Desktop GUI 版（Python tkinter）改寫為 **Web SaaS 系統**，使用者上傳平面圖、AI 自動辨識牆與材質、GA 演算法最佳化 AP 佈點、視覺化熱力圖、產出 PDF 報表。

---

## 一、專案背景

### 1.1 現況（Desktop 版已完成）
| 功能 | 狀態 |
|------|------|
| 平面圖照片上傳 | ✅ tkinter Canvas + Pillow |
| OpenCV 自動偵測牆 + 6 種材質 | ✅ HSV 顏色分群 |
| 比例尺校正 | ✅ 點兩點 + 輸入公尺 |
| 真 GA (NSGA-II) AP 佈點 | ✅ DEAP, NumPy 向量化（11s/13APs） |
| Auto-N 容量公式 | ✅ 1 AP / 120 m² |
| 牆體感知熱力圖（4 模式） | ✅ Signal / Coverage / Interference / SINR |
| 3D 訊號曲面、覆蓋率分析報表 | ✅ matplotlib |

### 1.2 Desktop 版的限制 → Web 化動機
| 痛點 | Web 化改善 |
|------|----------|
| 安裝 Python + 36 個套件門檻高 | 瀏覽器即用，零安裝 |
| Windows 限定 | 跨平台（含 iPad、手機） |
| 無多人協作 | 多使用者、專案分享、團隊權限 |
| 無雲端儲存 | 平面圖、結果、報表雲端保存 |
| 無歷史紀錄 | 規劃版本管理、變更追蹤 |
| 無 PDF 報表 | 一鍵產出客戶提案書 |
| 無法商業化 | 訂閱制、API 開放 |

---

## 二、系統架構

### 2.1 整體架構圖
```
┌──────────────────────────────────────────────────────┐
│                    使用者瀏覽器                        │
│  ┌────────────────────────────────────────────────┐  │
│  │  React 18 + TypeScript + Vite                  │  │
│  │  • 上傳區（拖放 PNG/JPG/PDF）                   │  │
│  │  • Canvas 編輯器（Konva.js）                   │  │
│  │  • 熱力圖渲染（D3.js / Plotly）                │  │
│  │  • 報表預覽（react-pdf-viewer）                │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────┬───────────────────────────────┘
                       │ REST + WebSocket
┌──────────────────────▼───────────────────────────────┐
│              FastAPI 後端 (Python 3.11)               │
│  ┌──────────────┬──────────────┬──────────────────┐  │
│  │ API Gateway  │ Auth (JWT)   │ Task Queue       │  │
│  │ (REST + WS)  │ (OAuth2)     │ (Celery + Redis) │  │
│  └──────────────┴──────────────┴──────────────────┘  │
└──────┬─────────────┬──────────────┬──────────────────┘
       │             │              │
┌──────▼────┐ ┌─────▼──────┐ ┌────▼─────────────────┐
│ CV 服務    │ │ GA 服務     │ │ PostgreSQL          │
│ • SAM 模型 │ │ • DEAP      │ │ + PostGIS (空間)    │
│ • OpenCV   │ │ • NumPy     │ │ + Redis (快取)      │
│ • PaddleOCR│ │ • 向量化    │ │ + S3 (圖檔/PDF)     │
└────────────┘ └─────────────┘ └──────────────────────┘
```

### 2.2 技術棧選型

#### 前端
| 元件 | 選型 | 理由 |
|------|------|------|
| Framework | **React 18 + TypeScript** | 型別安全、生態最大 |
| 建置工具 | Vite | HMR 快、Bundle 小 |
| Canvas | **Konva.js** | 平面圖編輯最成熟 |
| 圖表 | Plotly / D3 | 熱力圖互動式 |
| 狀態管理 | Zustand | 輕量、無樣板 |
| UI 組件 | shadcn/ui + Tailwind | 客製化彈性高 |
| 表單 | React Hook Form + Zod | 驗證強型別 |
| API 客戶端 | TanStack Query + Axios | 快取與 Loading 自動化 |

#### 後端
| 元件 | 選型 | 理由 |
|------|------|------|
| Framework | **FastAPI** | Python 原生、自動 OpenAPI、async |
| ORM | SQLAlchemy 2.0 + Alembic | 業界標準 |
| Auth | OAuth2 + JWT | 標準化、可整合 Google/MS SSO |
| Task Queue | **Celery + Redis** | GA 運算非同步、可水平擴展 |
| WebSocket | FastAPI WebSocket | GA 即時進度推送 |
| 物件儲存 | S3 / MinIO | 平面圖、PDF 永久儲存 |

#### AI / 演算法（沿用 Desktop 版）
| 套件 | 用途 |
|------|------|
| DEAP | GA NSGA-II 多目標最佳化 |
| OpenCV | 牆體偵測（HSV 顏色分群） |
| **Segment Anything (SAM)** | 真實照片牆體分割（新增） |
| PaddleOCR | 比例尺自動讀取（新增） |
| NumPy 向量化 RSSI | 物理傳播模型 |

#### DevOps
| 元件 | 選型 |
|------|------|
| 容器化 | Docker + Docker Compose（本地）/ K8s（生產） |
| CI/CD | GitHub Actions |
| 監控 | Prometheus + Grafana |
| 日誌 | Loki / Sentry |
| 雲端 | AWS / GCP（GPU 節點跑 SAM） |

---

## 三、核心功能模組

### 3.1 使用者旅程
```
[註冊/登入]
   ↓
[建立專案] — 命名、選範本（辦公/旅館/醫院/工廠...）
   ↓
[上傳平面圖] — 拖放 PNG/JPG/PDF, 自動讀取比例尺
   ↓
[AI 辨識牆與材質] — SAM + OpenCV (10-30 秒, 進度推送)
   ↓
[人工微調] — Canvas 編輯器修正牆段、材質、加門
   ↓
[參數設定] — 目標覆蓋率、人員密度、AP 機型
   ↓
[GA 最佳化] — 即時進度條 (~30-60 秒)
   ↓
[結果視覺化] — 熱力圖切換、3D 視角、覆蓋率分析
   ↓
[產出報表] — PDF 含平面圖 + AP 清單 + BoM
   ↓
[匯出/分享] — 連結分享、客戶簽核、修訂版本
```

### 3.2 主要 API 端點（v1）

```
POST   /api/v1/auth/login                  — JWT 登入
POST   /api/v1/auth/register                — 註冊新帳號

POST   /api/v1/projects                     — 新建專案
GET    /api/v1/projects                     — 列表
GET    /api/v1/projects/{id}                — 詳細
DELETE /api/v1/projects/{id}                — 刪除

POST   /api/v1/projects/{id}/floor-plans    — 上傳圖檔（multipart）
POST   /api/v1/projects/{id}/detect-walls   — 觸發 CV 偵測（async, returns task_id）
GET    /api/v1/tasks/{task_id}              — 查詢任務狀態（含進度）
WS     /api/v1/tasks/{task_id}/stream       — WebSocket 即時推送

PUT    /api/v1/projects/{id}/walls          — 人工修正牆列表
POST   /api/v1/projects/{id}/optimize       — 觸發 GA（async, target_coverage, num_aps...）

GET    /api/v1/projects/{id}/heatmap        — 取得熱力圖資料（JSON 矩陣）
GET    /api/v1/projects/{id}/report.pdf     — 下載 PDF 報表

POST   /api/v1/projects/{id}/share          — 產生分享連結
GET    /api/v1/projects/{id}/versions       — 版本歷史
```

### 3.3 資料模型（PostgreSQL）

```sql
users (id, email, password_hash, plan, created_at)
organizations (id, name, owner_id, plan)
projects (id, org_id, name, building_type, created_at)
floor_plans (id, project_id, image_url, scale_px_per_m, building_w, building_h)
walls (id, project_id, p1_x, p1_y, p2_x, p2_y, material, height)
access_points (id, project_id, x, y, z, model, tx_power_dbm, freq_ghz)
optimization_runs (id, project_id, algorithm, target_coverage,
                   final_coverage, num_aps, duration_sec, created_at)
heatmaps (id, project_id, run_id, grid_data_jsonb, mode)
reports (id, project_id, pdf_url, generated_at)
```

---

## 四、開發里程碑（12 週）

### Phase 1 — MVP 骨架（第 1-4 週）
- [ ] **W1**：Repo 初始化、Docker Compose 環境、CI/CD pipeline
- [ ] **W2**：FastAPI 骨架、PostgreSQL Schema、Alembic migration
- [ ] **W3**：React 骨架、Auth（註冊/登入/JWT）、Project CRUD
- [ ] **W4**：S3 上傳、Konva 平面圖檢視器（唯讀模式）

**交付**：可登入、可上傳圖、可顯示

### Phase 2 — 移植核心演算法（第 5-7 週）
- [ ] **W5**：把 Desktop 版 `wall_detector.py` 包成 Celery task
- [ ] **W6**：把 `genetic_optimizer.py` 包成 Celery task + WebSocket 進度
- [ ] **W7**：熱力圖計算 API + 前端 Plotly 渲染（4 種模式）

**交付**：上傳圖 → 跑 GA → 看熱力圖完整鏈路通

### Phase 3 — 互動編輯與校正（第 8-10 週）
- [ ] **W8**：Konva 平面圖編輯器（畫牆、選材質、放 AP）
- [ ] **W9**：比例尺校正 UI + 即時 RSSI 量測點功能
- [ ] **W10**：3D 視角（Three.js）+ 覆蓋率分析儀表板

**交付**：使用者可手動修正、互動式探索結果

### Phase 4 — 商業化功能（第 11-12 週）
- [ ] **W11**：PDF 報表（WeasyPrint）+ 多使用者分享
- [ ] **W12**：付費計畫、Stripe 整合、Beta 測試

**交付**：可上線收費的 SaaS

---

## 五、團隊配置

| 角色 | 人數 | 主要職責 |
|------|------|---------|
| 全端工程師 (Lead) | 1 | 架構、CI/CD、Code Review |
| 後端工程師 | 1 | FastAPI / Celery / DB |
| 前端工程師 | 1 | React / Konva / 視覺化 |
| AI 工程師 | 1 (兼) | SAM 整合、演算法調優 |
| UI/UX 設計師 | 0.5 | Wireframe / 視覺 |
| 產品 / 行銷 | 0.5 | 競品分析、Landing Page |

**總計 4-5 人 / 12 週**

---

## 六、預算估算（12 週）

| 項目 | 金額（NTD） |
|------|------------|
| 人力（4.5 人 × 12 週 × NT$ 50k/人月） | 2,700,000 |
| 雲端基礎設施（AWS EC2 + RDS + S3 + GPU） | 200,000 |
| 第三方授權（SAM 商用、Sentry、Stripe 手續費保證金） | 100,000 |
| UI / Logo / 設計外包 | 80,000 |
| 法務（隱私政策、條款、商標） | 50,000 |
| 行銷（SEO、Landing Page Ads） | 100,000 |
| **總計** | **約 3,230,000** |

---

## 七、商業模式

### 7.1 訂閱方案
| 方案 | 月費 | 上限 |
|------|------|------|
| **Free** | $0 | 1 樓層、200 m²、3 個專案 |
| **Pro** | NT$ 1,500/月 | 無樓層上限、PDF 報表、Email 支援 |
| **Team** | NT$ 5,000/月 | 5 人協作、版本歷史、API |
| **Enterprise** | 議價 | 私有部署、SSO、SLA |

### 7.2 一次性服務
- **PoC 顧問服務**：NT$ 30,000（含 1 場勘 + 規劃書）
- **客製化整合**：API 串接客戶現場 RSSI 量測 NT$ 100,000+

### 7.3 收益預估（12 個月）
| 月份 | Free | Pro | Team | 月營收 |
|------|------|-----|------|--------|
| M3 | 100 | 5 | 0 | $7.5k |
| M6 | 500 | 30 | 3 | $60k |
| M9 | 1500 | 80 | 10 | $170k |
| M12 | 3000 | 150 | 25 | $350k |

**第 12 個月年化營收 ≈ NT$ 4.2M**，預計 18 個月回本。

---

## 八、競爭分析

| 競品 | 價格 | 強項 | 我們的優勢 |
|------|------|------|----------|
| **Ekahau Pro** | USD 4,995/年 | 業界最完整、實機量測 | 1/10 價格、AI 自動辨識、零安裝 |
| **iBwave Design** | USD 10,000+/年 | 大型場域強 | 中小場域更便利、中文化 |
| **Hamina Network Planner** | USD 1,500/年 | UI 漂亮、Web 化 | **真 AI 自動辨識牆體（他們需手繪）** |
| **NetSpot** | USD 200 | 便宜、簡單 | 我們有 GA 最佳化（他們沒有） |

### 差異化護城河
1. **AI 自動辨識牆體 + 材質**（沒有競品做到）
2. **真實多目標 GA**（不只是經驗法則）
3. **中文化 + 在地建材資料庫**
4. **零安裝**（vs Ekahau / iBwave 桌面軟體）
5. **訂閱定價**（vs 競品 thousands USD 一次性）

---

## 九、風險與緩解

| 風險 | 等級 | 緩解策略 |
|------|------|---------|
| SAM 模型對手繪平面圖辨識率低 | 高 | 提供「半自動」回退模式，使用者點 + 確認 |
| GA 對大型場域（>2000 m²）變慢 | 中 | GPU 加速（CuPy）、分區計算 |
| 訊號模型與實測誤差 | 中 | 內建「現場校正」上傳實測 RSSI 反推材質參數 |
| 競品價格戰 | 中 | 走 Pro/Team 訂閱、做出 Ekahau 沒有的 AI 自動化 |
| 雲端 GPU 成本爆增 | 中 | Auto-scaling、Free 版限制圖大小、SAM 改 Lite 版 |
| 個資 / GDPR 合規 | 中 | 圖檔加密儲存、私有部署選項、台灣資料中心 |

---

## 十、立即下一步（接下來 2 週）

### Week 1：環境與骨架
1. **Day 1-2**：建立 GitHub Repo（monorepo 或拆 frontend/backend）
2. **Day 3-4**：Docker Compose（PostgreSQL + Redis + MinIO 本地）
3. **Day 5-7**：FastAPI Hello World + React Vite Hello World，全部能跑

### Week 2：開始移植
1. **Day 8-9**：把 Desktop 版 `wall_detector.py` 與 `genetic_optimizer.py` 包成 Python package
2. **Day 10-11**：用 Celery 包成 task，FastAPI 觸發
3. **Day 12-14**：前端最簡上傳介面 + 顯示偵測到的牆（純 JSON 顯示）

---

## 附錄 A：複用 Desktop 版的程式碼清單

| Desktop 檔案 | Web 端用法 |
|------------|-----------|
| `src/genetic_optimizer.py` | 100% 複用，包成 Celery task |
| `src/wall_detector.py` | 100% 複用，加 SAM 模式 |
| `floor_plans/*.png` | 變成測試 fixture |
| `gui/run_gui.py` | 廢棄，改 React |
| `gui/wifi_gui_app.py` | 視覺化邏輯抽出，剩下 tkinter 廢棄 |
| `MATERIAL_COLORS` / `WALL_ATTEN` | 移到 shared/constants 共用 |

---

## 附錄 B：技術問答

**Q1：為何選 FastAPI 而非 Django？**
FastAPI 原生 async、自動產生 OpenAPI 文件、跟現有 Python 演算法 100% 相容、輕量適合微服務。

**Q2：SAM 模型成本？**
SAM ViT-H 約 2.5 GB，需要 GPU（推論 ~1-3 秒/張）。
替代方案：SAM-Lite (~50MB CPU 可跑) 或保留 OpenCV 對清晰平面圖。

**Q3：GA 在 Web 端要跑多久？**
複用 Desktop 向量化版本：
- 中型辦公 (40×30m, 6-8 AP)：5-15 秒
- 大型 (50×50m, 12-15 AP)：30-60 秒
搭配 WebSocket 即時推送進度，使用者體感 OK。

**Q4：可以離線使用嗎？**
MVP 不支援。第 6 個月可考慮 PWA + WebAssembly 跑 GA。
